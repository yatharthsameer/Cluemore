<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime entitlements for Electron apps -->
    
    <!-- Allow JIT compilation (required for V8/Chromium) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow unsigned executable memory (required for V8) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Allow DYLD environment variables (required for Electron) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    
    <!-- Disable library validation (required for native modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Network access for web content -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Network server (if app acts as server) -->
    <key>com.apple.security.network.server</key>
    <true/>
    
    <!-- File access (read-only by default) -->
    <key>com.apple.security.files.user-selected.read-only</key>
    <true/>
    
    <!-- Camera access (if needed) -->
    <key>com.apple.security.device.camera</key>
    <true/>
    
    <!-- Microphone access (if needed) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist> 
